import { FC } from "react";
import { Typography } from "geoiq-frontend-ui-kit";

const ChessHubPage: FC = () => {
  return (
    <div>
      <Typography variant="h1" className="text-light-1">
        Chess Hub
      </Typography>
      Chess Hub
    </div>
  );
};

export default ChessHubPage;
